{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% spaceless %}
        {%- if value is empty -%}
            &nbsp;
        {%- else -%}
            {{ value|join(', ') }}
        {%- endif -%}
    {% endspaceless %}
{% endblock %}