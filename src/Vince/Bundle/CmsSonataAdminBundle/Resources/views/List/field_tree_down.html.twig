{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% spaceless %}
        {% if object.parent and object.parent.children[object.parent.children|length - 1].id != object.id %}
            <a href="{{ path('admin_vince_cms_menu_down', {'id': object.id}) }}">
                <img src="{{ asset('bundles/sonataadmin/famfamfam/arrow_down.png') }}" />
            </a>
        {% endif %}
    {% endspaceless %}
{% endblock %}