{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% spaceless %}
        {%- if value is empty -%}
            &nbsp;
        {%- else -%}
            {{ value|number_format(2, ',', ' ') }} €
        {%- endif -%}
    {% endspaceless %}
{% endblock %}