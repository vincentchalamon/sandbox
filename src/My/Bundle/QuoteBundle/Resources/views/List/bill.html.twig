{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% spaceless %}
        <a href="{{ path('download_bill', {'numero': object.numero|slice(0, -1)}) }}">
            <i class="icon-file"></i>
        </a>&nbsp;
        <a href="#sendBill{{ object.id }}" role="button" data-toggle="modal">
            <i class="icon-envelope"></i>
        </a>
    {% endspaceless %}
{% endblock %}