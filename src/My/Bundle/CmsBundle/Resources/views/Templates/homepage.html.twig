{% extends 'MyCmsBundle::base.html.twig' %}

{% block body %}
    <h1>{{ block('title') }}</h1>

    <div class="contents">{{ article|content('main') }}</div>

    {{ render_block('homepage') }}
{% endblock %}